# Figma MCP 용어 사전 — 비전문가를 위한 완전 해설

> 개발자가 아닌 **디자인을 지시하는 사람** 시점으로 작성

---

## 전체 연결 구조

```
Claude Code (AI)  ←→  MCP (통역사)  ←→  Figma (디자인 도구)
     말로 지시            번역              시각적 결과
```

**핵심: Claude Code와 Figma는 서로 언어가 다름. MCP가 통역사 역할.**

---

## Part 1. Figma 기본 용어

| 용어 | 쉬운 설명 | 비유 |
|------|---------|------|
| **File** | Figma 파일 하나 | 파워포인트 파일 |
| **Page** | 파일 안의 탭 | PPT의 슬라이드 묶음 |
| **Frame** | 화면이나 영역을 감싸는 박스 | 액자 |
| **Node** | Figma 안의 모든 요소 (프레임, 텍스트, 이미지...) | 레고 블록 |
| **Node ID** | 각 요소의 고유 번호 (예: `123-456`) | 주민등록번호 |
| **Layer** | 요소가 쌓이는 순서 | 포토샵 레이어 |
| **Component** | 재사용 가능한 UI 조각 (버튼, 카드, 헤더) | 도장 (찍으면 같은 게 나옴) |
| **Instance** | 컴포넌트를 복사해서 쓴 것 | 도장으로 찍은 결과물 |
| **Variant** | 컴포넌트의 변형 (큰 버튼, 작은 버튼, 비활성 버튼) | 도장 세트 |

## Part 2. 디자인 속성 용어

| 용어 | 쉬운 설명 | 예시 |
|------|---------|------|
| **Fill** | 배경색/채우기 | 버튼의 파란색 배경 |
| **Stroke** | 테두리선 | 입력창의 회색 테두리 |
| **Opacity** | 투명도 | 0% = 안 보임, 100% = 완전 보임 |
| **Corner Radius** | 모서리 둥글기 | `8px` = 약간 둥글게 |
| **Auto Layout** | 자동 정렬 | CSS의 Flexbox/Grid와 같은 것 |
| **Padding** | 안쪽 여백 | 버튼 테두리와 글자 사이 간격 |
| **Gap** | 요소 사이 간격 | 카드들 사이의 빈 공간 |
| **Constraints** | 크기 변할 때 어떻게 반응할지 | "왼쪽에 붙어있어" / "늘어나" |

## Part 3. 디자인 토큰/변수 용어

| 용어 | 쉬운 설명 | 비유 |
|------|---------|------|
| **Variable** | 이름 붙인 값 (색상, 크기 등) | `primary-blue = #3B82F6` |
| **Token** | Variable의 다른 이름 | 같은 것 |
| **Collection** | 변수들의 묶음 | "색상 모음", "간격 모음" |
| **Mode** | 같은 변수의 다른 상태 | 라이트 모드 / 다크 모드 |
| **Design System** | 토큰 + 컴포넌트 + 규칙의 총합 | 브랜드 가이드북 |

## Part 4. MCP 연결 용어

| 용어 | 쉬운 설명 |
|------|---------|
| **MCP** | AI와 외부 도구를 연결하는 프로토콜 (통역 규약) |
| **MCP Server** | 통역사 프로그램 (Figma용, GitHub용, 등) |
| **Tool** | MCP가 할 수 있는 개별 기능 (읽기, 쓰기, 검색) |
| **Skill** | 도구를 어떤 순서로 쓸지 정리한 레시피 |
| **OAuth** | "Figma에 로그인해도 될까요?" 허가 과정 |
| **PAT (Personal Access Token)** | API 접근용 비밀번호 |
| **WebSocket** | 실시간 양방향 통신 (전화 통화처럼) |
| **Rate Limit** | 시간당 사용 횟수 제한 |

## Part 5. URL 구조

```
https://www.figma.com/design/ABC123/My-Design?node-id=123-456&mode=dev
                              ^^^^^^          ^^^^^^^^^        ^^^^^^^^
                              파일키          노드ID           Dev모드
```

| 부분 | 의미 |
|------|------|
| `ABC123` | 파일 고유 키 |
| `node-id=123-456` | 특정 요소 지정 |
| `&mode=dev` | Dev Mode 링크 (코드 변환 품질 최고) |

## Part 6. 코드 변환 용어

| 용어 | 쉬운 설명 |
|------|---------|
| **React** | UI를 컴포넌트로 만드는 프레임워크 |
| **Tailwind** | CSS를 클래스 이름으로 쓰는 방식 (`bg-blue-500`) |
| **TypeScript** | JavaScript에 타입(종류) 검사를 추가한 것 |
| **Responsive** | 화면 크기에 맞게 자동 조절 |
| **Flexbox/Grid** | 요소를 가로/세로/격자로 배치하는 방법 |
| **CSS Custom Property** | `--color-primary` 같은 CSS 변수 |

## Part 7. 자주 막히는 3가지 해결법

### Node ID를 모를 때
```
"이름에 Button이 들어간 거 다 찾아줘"
→ AI가 검색해서 목록 + Node ID 알려줌
→ ID를 외울 필요 없음
```

### 결과를 눈으로 확인하고 싶을 때
```
"스크린샷 찍어서 보여줘"
→ 중간중간 넣으면 AI가 바꾼 결과를 이미지로 확인 가능
```

### 일괄 변경이 무서울 때
```
"먼저 목록만 보여주고, 확인 후에 실행해"
→ 두 단계로 나누면 안전
```

## Part 8. 실전 문장 예시

이 용어들을 조합하면 이렇게 정확한 지시가 됩니다:

```
"이 Frame 안에 있는 Button Component의 Fill을 primary-blue Variable로 바꿔줘"
  ^^^^            ^^^^^^              ^^^^      ^^^^^^^^^^^^ ^^^^^^^^
  영역              요소 종류           배경색     토큰 이름       변수
```

```
"Header Frame의 Gap을 24px로 바꾸고, Padding을 상하 16px 좌우 32px로 설정해줘"
```

```
"로그인 Page에서 모든 Button Instance의 Corner Radius를 12px로 통일해줘"
```

```
"이 Design System의 color Collection에서 Light Mode와 Dark Mode 값을 비교해줘"
```

---

> **용어를 몰라도 자연어로 말하면 AI가 이해합니다.**
> 하지만 용어를 알면 **더 정확하고 빠르게** 결과를 얻습니다.
