# Figma MCP 워크플로우 & 프롬프트 패턴

> 2026-04-03 — 07-complete-guide-claude-code(워크플로우) + 09-prompt-patterns 통합

---

## 워크플로우 A–H

### A. 기존 디자인 → 코드 (가장 흔함)
```
1. Figma에서 프레임 선택 → Cmd+L로 링크 복사
2. Claude Code: "이 디자인을 구현해줘 [링크]"
3. get_design_context가 구조화된 데이터 추출
4. 코드 생성 → 리뷰 → 수정
```

### B. 새 디자인 생성 (use_figma)
```
1. 빈 Figma 파일 URL 준비
2. Claude Code: "로그인 화면 만들어줘 [URL]"
3. use_figma가 캔버스에 직접 생성
4. Figma에서 확인 → 후속 수정 요청
```

### C. 디자인 시스템 동기화
```
1. Figma Variables 추출: get_variable_defs
2. Tailwind config / CSS variables로 변환
3. 코드베이스에 적용
```

### D. 풀 파이프라인 (최강 조합)
```
Figma MCP로 초안 → Claude Code로 코드 변환 → Agentation으로 실제 앱 수정
```

### E. 아이디어 → Figma 초안 → 코드
```
# 1단계 — Figma 초안 요청
"Figma 파일 [URL]에 SaaS 대시보드 메인 화면 초안을 만들어줘.
서비스: AI 자동화 도구 / 대상: 1인 사업가
포함: 상단 네비게이션, 왼쪽 사이드바, KPI 카드 4개, 최근 활동 테이블
색상: 흰색 배경, 파란색 포인트 / 크기: 1440x900px"

# 2단계 — 마음에 들면 코드 변환
"방금 만든 Figma 대시보드 디자인을 읽어서
Next.js + Tailwind CSS 코드로 변환해줘. 컴포넌트 단위로 분리해줘."
```
> 코드 없이 레이아웃/색상 테스트 → 팀·클라이언트 공유 → 일관성 확인

### F. 레퍼런스 가져와서 수정 (가장 현실적)
```
1. 레퍼런스 사이트 발견
2. generate_figma_design으로 Figma에 가져오기
   → "이 웹사이트를 Figma 디자인으로 변환해줘 [웹 URL]"
3. Dev Mode에서 구조/CSS/레이어 확인
4. use_figma로 필요한 부분 수정
   → "이 프레임에서 색상을 브랜드 컬러로 바꾸고, 헤더 텍스트 수정해줘"
5. get_design_context로 최종 코드 변환
   → "이 디자인을 Next.js + Tailwind로 구현해줘"
```

**캡처 vs MCP 비교:**

| 방식 | 가져오는 것 | 수정 방식 | 정확도 |
|------|-----------|----------|--------|
| 스크린샷 캡처 | 이미지(픽셀) | AI가 추측해서 재구성 | 낮음 |
| MCP (`generate_figma_design`) | 요소값(레이어, CSS, 토큰) | 구조 그대로 수정 | 높음 |

### G. 여러 화면 플로우 정리
```
Figma [URL]에 아래 화면들을 순서대로 나란히 만들어줘.
각 화면은 [크기], 간격 [px]로 배치.

화면 1 - [이름]: [요소 나열]
화면 2 - [이름]: [요소 나열]
화면 3 - [이름]: [요소 나열]

전체를 하나의 플로우로 볼 수 있게 정리해줘.
```

### H. 원격 협업 (Copy link to selection + 코멘트)
```
# 선택 요소 공유
Figma에서 요소 선택 → 우클릭 → "Copy link to selection"
→ 팀원에게 공유 또는 Claude Code에 붙여넣기

# 코멘트 활용
Figma 코멘트에 @Claude 또는 프롬프트 메모 → MCP로 코멘트 읽어서 처리
→ "이 URL의 코멘트를 읽고 요청된 수정사항 반영해줘"
```

---

## 프롬프트 패턴 15개 (A–O)

### 패턴 A. 새 화면 만들기
```
Figma [URL]에 [화면 이름] 화면 만들어줘.
배경: [색상]
포함할 요소: [항목 나열]
분위기: [키워드]
```

### 패턴 B. 기존 요소 수정
```
Figma [URL]의 [요소 이름]에서
[속성]을 [현재값]에서 [새 값]으로 바꿔줘
```

### 패턴 C. 섹션 추가
```
Figma [URL]의 [기존 프레임] 아래에 새 섹션 추가해줘:
- 제목: [텍스트]
- 구조: [레이아웃 설명]
- 크기: [width]x[height]
```

### 패턴 D. 레퍼런스 사이트 가져오기
```
이 웹사이트를 Figma 디자인으로 변환해줘:
[웹사이트 URL]

Figma 파일: [Figma URL]
별도 페이지에 넣어줘.
```

### 패턴 E. 여러 화면 플로우 생성
```
Figma [URL]에 아래 화면들을 순서대로 나란히 만들어줘.
각 화면은 [크기], 간격 [px]로 배치.
화면 1~N 목록 + 전체를 하나의 플로우로 정리해줘.
```

### 패턴 F. 와이어프레임 → 디자인 입히기
```
# 1단계: 와이어프레임
Figma [URL]에 랜딩페이지 와이어프레임(회색 박스 스케치)을 만들어줘. 전체 너비 1440px.

# 2단계: 디자인 입히기 (확정 후)
이 와이어프레임에 색상이랑 폰트 입혀줘.
색상: [메인색] + [포인트색] / 폰트: [폰트명] / 분위기: [키워드]
```

### 패턴 G. 디자인 → 코드 변환
```
이 Figma 디자인을 [프레임워크]로 구현해줘.
[Figma 노드 URL (Cmd+L)]
컴포넌트 단위로 분리해줘. 반응형으로 만들어줘.
```

### 패턴 H. 디자인 토큰 추출
```
이 Figma 파일의 디자인 토큰을 추출해줘: [Figma URL]
색상, 간격, 타이포그래피 변수를 Tailwind config 형식으로 변환해줘.
```

### 패턴 I. 기존 코드 → Figma 업로드
```
이 프로젝트의 [페이지]를 Figma에 디자인으로 생성해줘.
Figma 파일: [URL] / 참고 코드: [폴더 또는 파일 경로]
Tailwind 클래스 기반으로 색상/간격/폰트를 그대로 Figma 프레임으로 옮겨줘.
```

### 패턴 J. 컴포넌트 수정/교체
```
Figma [URL]에서 [컴포넌트 이름]을 수정해줘:
- 모서리: 둥글게 (radius 12px)
- 그림자: 추가 (shadow-lg 수준)
- 패딩: 상하 16px, 좌우 24px
```

### 패턴 K. 반응형 변형 생성
```
Figma [URL]의 데스크탑 디자인(1440px)을
모바일 버전(375px)으로도 만들어줘. 같은 페이지에 나란히 배치.
```

### 패턴 L. 레퍼런스 섹션별 조합
```
아래 사이트들의 특정 섹션을 조합해서 새 디자인을 만들어줘:
- 히어로: [사이트1 URL] / 기능 소개: [사이트2 URL]
- 가격표: [사이트3 URL] / 소셜 프루프: [사이트4 URL]
Figma 파일: [URL] / 전체 톤: [색상/분위기]
```

### 패턴 M. 비교/분석 요청
```
Figma [URL]에 있는 3개 페이지의 섹션 구조를 비교 분석해줘.
- 각 사이트의 섹션 목록 / 공통 패턴 / 각각의 강점
- 우리 서비스에 맞는 베스트 조합 추천
```

### 패턴 N. 트렌드 보완
```
이 디자인을 2026년 [분야] 트렌드에 맞게 업그레이드해줘:
벤토 그리드, 글래스모피즘 카드, 실시간 카운터, Before/After 비교,
스티키 CTA 바, 다크 모드 대응 [Figma URL]
```

### 패턴 O. 디자인 시스템 구축
```
이 Figma 파일에 디자인 시스템 기초를 만들어줘: [Figma URL]
포함: 색상 팔레트 (Primary/Secondary/Neutral/Semantic)
타이포그래피 스케일 (H1~H6, Body, Caption)
간격 체계 (4px 기반), 버튼 변형, 입력 필드, 카드 컴포넌트
```

---

## 빠른 참조표

| 하고 싶은 것 | 워크플로우 | 패턴 |
|-------------|-----------|------|
| Figma 디자인 → 코드 | A | G |
| 새 화면 생성 | B | A |
| 디자인 토큰 동기화 | C | H |
| 전체 앱 파이프라인 | D | — |
| 아이디어 → Figma → 코드 | E | A+G |
| 레퍼런스 가져와서 수정 | F | D+B |
| 앱 플로우 한눈에 | G | E |
| 팀 협업/코멘트 | H | — |
| 섹션 추가 | — | C |
| 코드 → Figma | — | I |
| 컴포넌트 수정 | — | J |
| 반응형 변형 | — | K |
| 레퍼런스 조합 | — | L |
| 비교 분석 | — | M |
| 트렌드 보완 | — | N |
| 디자인 시스템 구축 | — | O |

---

## 작업 명시 템플릿 (복붙용)

> Figma URL 뒤에 아래 작업 중 하나를 붙여서 사용

### 화면 생성
```
[URL] 여기에 [서비스명] 랜딩페이지 만들어줘.
히어로 + 기능 소개 3열 + 가격 플랜 3개 + 후기 + 푸터 포함.
색상: [메인색], 크기: 1440px
```

### 모바일 앱 플로우
```
[URL] 여기에 모바일 앱 5개 화면 만들어줘 (375x812px, 간격 80px):
스플래시 → 온보딩 → 로그인 → 홈 → 설정
```

### 컴포넌트 세트
```
[URL] 여기에 디자인 시스템 기초 만들어줘:
버튼(Primary/Secondary/Ghost), 인풋, 카드, 배지, 네비게이션
```

### 레퍼런스 가져오기
```
이 웹사이트를 Figma로 가져와줘: [웹 URL]
Figma 파일: [URL]
```

### 기존 디자인 수정
```
[URL]의 히어로 섹션에서:
- 헤드라인 텍스트를 "[새 텍스트]"로 변경
- CTA 버튼 색상을 [#색상코드]로 변경
- 배경 이미지 자리 추가
```

### 코드 변환
```
[URL] 이 디자인을 [React/Vue/HTML+CSS/Tailwind]로 변환해줘.
컴포넌트 단위로 분리. 반응형 포함.
```

### 디자인 토큰 추출
```
[URL] 이 파일의 색상/간격/타이포 변수를 Tailwind config로 변환해줘.
```

### 비교 분석
```
[URL] 이 파일의 3개 페이지를 비교해서:
각 섹션 구조, 공통 패턴, 베스트 조합 추천해줘.
```
